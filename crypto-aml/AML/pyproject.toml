[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "aml-pipeline"
version = "0.1.0"
description = "Wallet clustering ETL pipeline"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
